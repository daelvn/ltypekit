-- alfons: moon
export clean   ==>
  os.execute "rm -r **/*.lua *.lua"
  os.execute "rm -r ltypekit/*.moon"
  os.execute "rm -r ltypekit/*/*.moon"
export compile ==>
  os.execute "msmx ltypekit/*.msmx"
  os.execute "msmx ltypekit/*/*.msmx"
  os.execute "moonc ."
